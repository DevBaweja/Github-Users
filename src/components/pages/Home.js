import React, { Fragment } from 'react';
import Search from '../Utils/Search';
import Users from '../User/Users';

const Home = () => (
    <Fragment>
        <Search />
        <Users />
    </Fragment>
);

export default Home;
